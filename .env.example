# Copy to .env and fill in real values — never commit .env itself.
APP_ENV=development
APP_PORT=8000
DATABASE_URL=postgresql://user:password@localhost:5432/projectmono
SECRET_KEY=change-me-to-a-random-string
LOG_LEVEL=INFO
